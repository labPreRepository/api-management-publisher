_schema-version: "3.2"
ID: bungehubapi
description: Generated by Fiori Tools
version: 0.0.2
modules:
- name: bungehubapi-destination-content
  type: com.sap.application.content
  requires:
  - name: bungehubapi-destination-service
    parameters:
      content-target: true
  - name: bungehubapi-repo-host
    parameters:
      service-key:
        name: bungehubapi-repo-host-key
  - name: bungehubapi-uaa
    parameters:
      service-key:
        name: bungehubapi-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: bungehubapi_html_repo_host
          ServiceInstanceName: bungehubapi-html5-srv
          ServiceKeyName: bungehubapi-repo-host-key
          sap.cloud.service: bungehubapi
        - Authentication: OAuth2UserTokenExchange
          Name: bungehubapi_uaa
          ServiceInstanceName: bungehubapi-xsuaa-srv
          ServiceKeyName: bungehubapi-uaa-key
          sap.cloud.service: bungehubapi
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: bungehubapi-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: bungehubapi-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - bungehubapi.zip
      name: bungehubapi
      target-path: resources/
- name: bungehubapi
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: bungehubapi-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: bungehubapi-destination-service
    service-plan: lite
- name: bungehubapi-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: bungehubapi-xsuaa-srv
    service-plan: application
- name: bungehubapi-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: bungehubapi-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
